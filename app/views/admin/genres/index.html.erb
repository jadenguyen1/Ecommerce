<p style="color: green"><%= notice %></p>

<h1 class="text-center" style="font-family: 'Merriweather', serif; color: #4d3f2b;">Manage Genres</h1>

<div class="container">
  <%= link_to "New Genre", new_admin_genre_path, class: "btn btn-success my-3" %>
  <div class="table-responsive">
    <table class="table table-striped table-bordered table-sm">
      <thead>
        <tr>
          <th style="width: 10%;">ID</th>
          <th style="width: 75%;">Name</th>
          <th style="width: 15%;">Actions</th>
        </tr>
      </thead>
      <tbody>
        <% @genres.each do |genre| %>
          <tr>
            <td><%= genre.id %></td>
            <td><%= genre.name %></td>
            <td>
              <div class="d-flex">
                <%= link_to "Edit", edit_admin_genre_path(genre), class: "btn btn-warning btn-sm me-2" %>
                <%= button_to "Delete", admin_genre_path(genre), method: :delete, class: "btn btn-danger btn-sm", data: { confirm: "Are you sure?" } %>
              </div>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
