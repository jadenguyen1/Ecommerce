<h2>Order #<%= @order.id %> Details</h2>
<p><strong>Order Date:</strong> <%= @order.order_date.strftime("%B %d, %Y") %></p>
<p><strong>Total Price:</strong> <%= number_to_currency(@order.total_price) %></p>

<h3>Books in this Order:</h3>
<ul>
  <% @order_items.each do |item| %>
    <li><%= item.book.title %> - <%= item.quantity %> x <%= number_to_currency(item.unit_price) %></li>
  <% end %>
</ul>
